apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: innovatech
spec:
  type: LoadBalancer
  selector:
    app: front-despacho
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
